# specs（输入配置）

本目录用于存放本项目的输入配置（geometry/case/sampling/airfoils），目标是让项目**不依赖**旧的 `ref_enthalpy/specs`。

目前已内置一套最小 demo specs（足够跑通 `scripts/run_case_rem.py` 默认参数）。

本目录当前已经包含一套完整 specs（已从旧实现迁移）。

如你仍保留旧目录 `ref_enthalpy/`，现在也不会再被新项目依赖，可自行删除以减少干扰。

